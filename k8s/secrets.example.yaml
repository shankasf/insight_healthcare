# Template only. Do NOT commit real secrets. Apply the real secret manually with:
#
#   kubectl create secret generic insight-app-secrets \
#     -n insight-healthcare \
#     --from-literal=OPENAI_API_KEY=sk-... \
#     --from-literal=DATABASE_URL=postgresql+asyncpg://postgres:<pw>@72.62.162.83:5432/insight_healthcare
#
# This file exists so other engineers know which keys the deployments expect
# under `insight-app-secrets` via `secretKeyRef`. Values below are placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: insight-app-secrets
  namespace: insight-healthcare
  labels:
    app: insight-healthcare
type: Opaque
stringData:
  OPENAI_API_KEY: "sk-REPLACE_ME"
  DATABASE_URL: "postgresql+asyncpg://postgres:REPLACE_ME@72.62.162.83:5432/insight_healthcare"
